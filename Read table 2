create table employees(id int primary key, fname varchar(10), lname varchar(10));
insert into employees values (1, 'ram','kumar');
insert into employees values (2, 'ravi','kumar');
select*from employees;
select fname "First Name", lname "Last Name" from employees;
